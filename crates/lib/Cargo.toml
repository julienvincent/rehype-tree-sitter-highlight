[package]
name = "rehype-tree-sitter-nodelib"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]
name = "highlight"

[dependencies]
napi = "3.0.0"
napi-derive = "3.0.0"

rehype-tree-sitter-highlight = { path = "../highlight/" }

[build-dependencies]
napi-build = "2"
